set nocompatible " set uncompatibality with vi

	" setting mapleader here, in case I use them in Plugin mappings
let mapleader=","
let maplocalleader=";"




so ~/.vim/config/plugin
so ~/.vim/config/settings
so ~/.vim/config/colour
so ~/.vim/config/functions
so ~/.vim/config/mappings/normal
so ~/.vim/config/mappings/insert
so ~/.vim/config/mappings/command
so ~/.vim/config/mappings/leader
so ~/.vim/config/autocmds
